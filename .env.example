# Environment variables for Minimal Wallet Connect Project

# Set NODE_ENV to 'development' or 'production'
NODE_ENV=development

# Wallet connect settings (add as needed)
WALLET_CONNECT_PROJECT_ID=

# Neon DB (production only)
NEON_DB_URL=

# Set to 'true' to skip DB connection in dev mode
SKIP_DB=true
